function setup() {
  createCanvas(400, 400);
}

function draw() {
  if(mouseIsPressed === true) {
    fill(0);
    
    
  }else {
    fill(2550);
  }
  
  
  // blah a circle 
  
  circle(mouseX,mouseY,100);
}